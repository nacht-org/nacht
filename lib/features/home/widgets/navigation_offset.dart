import 'package:flutter/material.dart';


class NavigationOffset extends StatelessWidget {
  const NavigationOffset({super.key});
  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 80.0);
  }
}
